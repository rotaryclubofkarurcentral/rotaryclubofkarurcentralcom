# Rotary Club of Karur Central

This repository now uses VitePress as the main site.

Local commands:

```
npm install
npm run dev       # starts VitePress dev server
npm run build     # builds static site into .vitepress/dist
npm run start     # preview the built site
```

Site config: `.vitepress/config.js`.
